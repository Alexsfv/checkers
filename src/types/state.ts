export type GlobalMessage = {
    created_at: string
    text: string
    user: {
        image: string
        nickName: string
        _id: string
    } | null
    __v: number
    _id: string
}

